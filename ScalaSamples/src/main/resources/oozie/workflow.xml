<workflow-app name="test_app" xmlns="uri:oozie:workflow:0.4">
 <start to="stage_data_to_local"/>
 <action name="stage_data_to_local">
   <ssh xmlns="uri:oozie:ssh-action:0.1">
      <host>oozie@localhost</host>
      <command>/home/cloudera/OOZIE/oozie_scripts/sample_shell.sh</command>
   </ssh>
 <ok to="end" />
 <error to="kill" />
 </action>
 <kill name="kill">
 <message>Action failed, error message #[[[${wf:errorMessage(wf:lastErrorNode())}]]]#</message>
 </kill>
 <end name="end"/>
 </workflow-app>

